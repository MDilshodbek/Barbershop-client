$ErrorActionPreference = "Stop"

# Write-Host "==> Resetting local repository to clean state"
# git reset --hard

# Write-Host "==> Checking out deployment branch: master"
# git checkout master

# Write-Host "==> Pulling latest code from origin/master"
# git pull origin master

docker compose up -d